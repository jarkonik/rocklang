memset = (vec, val, n) => {
	i = 0
	while i < n {
		vec = vecset(arr, i, val)
		i = i + 1
	}
	vec
}

sieve = (n) => {
	prime = memset(vecnew(), true, n + 1)

	p = 2

	while p * p <= n {
		if vecget(prime, p) {
			i = p * p
			while i <= n {
				prime = vecset(prime, i, false)
				i = i + p
			}
		}

		p = p + 1
	}

	p = 2

	while p <= n {
		if vecget(prime, p) {
			print(p)
			print("\n")
		}

		p = p + 1
	}
}

sieve(7)
